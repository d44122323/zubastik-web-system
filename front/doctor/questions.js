let items=[];const esc=s=>String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
async function me(){const r=await fetch('/api/doctor/me',{credentials:'include'});if(!r.ok){location='/?account=doctor';return null}const d=await r.json();document.getElementById('doctorName').textContent=d.name;return d}
async function load(){const r=await fetch('/api/doctor/questions',{credentials:'include'});if(!r.ok){alert(await r.text());return}items=await r.json();render()}
function render(){const box=document.getElementById('list');if(!items.length){box.innerHTML='<div class="empty">Вопросов от пациентов пока нет.</div>';return}box.innerHTML=items.map(q=>`<article class="question"><div class="row"><div><h3>${esc(q.patient)}</h3><div class="muted">${esc(q.service)} · ${esc(q.createdAt)}</div></div><span class="tag">${q.answer?'Отвечено':'Ожидает ответа'}</span></div><p><b>Вопрос:</b><br>${esc(q.question)}</p>${q.answer?`<div class="answer"><b>Ваш ответ:</b><p>${esc(q.answer)}</p></div>`:`<div class="answer-form"><textarea id="answer-${q.id}" maxlength="4000" placeholder="Напишите ответ пациенту..."></textarea><button class="primary" onclick="answerQuestion(${q.id})">Ответить</button></div>`}</article>`).join('')}
async function answerQuestion(id){const ta=document.getElementById('answer-'+id);const answer=ta.value.trim();if(!answer){alert('Введите ответ');return}const r=await fetch('/api/doctor/questions/'+id,{method:'PUT',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({answer})});if(!r.ok){alert(await r.text());return}await load()}
document.getElementById('logout').onclick=async()=>{await fetch('/doctor/logout',{method:'POST',credentials:'include'});location='/?account=doctor'};me().then(load);
